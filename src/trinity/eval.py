"""Entrypoint: evaluate a trained coordinator on the benchmark suite.

Usage:
    python -m trinity.eval --config configs/benchmarks.yaml
"""
from __future__ import annotations
# Placeholder — wires session + benchmarks once SPEC lands.
